const Action = require('./modules/action.js').default
const Importer = require('./modules/importer.js').default
const IntegrationWidget = require('./modules/integration-widget.js').default

export default {
    Action,
    Importer,
    IntegrationWidget,
}
